from setuptools import setup, find_packages

setup(
    name="ai_impact",
    version="0.0.1",
    author="tamimystic",
    author_email="example@example.com",
    packages=find_packages(),
    install_requires=[],
)
